{
  "name": "@baanthong/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'shared is consumed via workspace path; no build step' && exit 0",
    "test": "bun test"
  }
}
