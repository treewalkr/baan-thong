{
  "name": "baan-thong",
  "private": true,
  "version": "0.0.0",
  "description": "baan-thong — full-stack TypeScript monorepo (Phase 1: modular monolith).",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "echo \"run via: just dev\" && exit 1",
    "build": "bun run --filter '*' build",
    "lint": "biome check .",
    "format": "biome format ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.1",
    "@types/bun": "latest",
    "typescript": "^5.9.2"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
