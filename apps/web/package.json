{
  "name": "@baanthong/web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "echo \"SvelteKit scaffold comes in a later issue\" && exit 0",
    "build": "echo \"SvelteKit scaffold comes in a later issue\" && exit 0",
    "preview": "echo \"SvelteKit scaffold comes in a later issue\" && exit 0",
    "test": "bun test"
  },
  "dependencies": {
    "@baanthong/shared": "workspace:*"
  }
}
